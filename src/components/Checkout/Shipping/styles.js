import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
    formControl: {
        width: "100%",
    },
    buttons: {
        display: "flex",
        justifyContent: "space-evenly",
    },
}));
